/// @desc Bounce

hits++;

// var xx = irandom_range(100, 1500);
// var yy = irandom_range(100, 800);

if (hits % 1 == 0) {
	// sprite_index = spr_Player_Small;
	// obj_Controller.alarm[0] = game_get_speed(gamespeed_fps);
	// var powerup = instance_create_layer(xx, yy, obj_Controller.layer_Game, obj_Powerup);
}
